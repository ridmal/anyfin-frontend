const Config = {
  graphQLServer: '', // graphQL query endpoint ex: http://localhost:4000/anyfin
  authProviderDomain: '', // Auth provider domain ( Ex: Auth0 application domain )
  authProviderClientId: '', // Auth provide client id ( Ex: Auth0 client id )
  authProviderAudience: '', // Auth provide audience ( End point which handles authentication, ex: http://localhost:4000 )
};

export default Config;
